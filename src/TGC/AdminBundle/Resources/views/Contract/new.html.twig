{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Contract creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('contract') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
