{% extends 'TGCAdminBundle::formlayout.html.twig' %}

{% block title -%}Search for Projects{% endblock %}

{% block content -%}
&nbsp;
    {{ form(form) }}


{% if entities is null %}
<p>&nbsp;</p>
<p>&nbsp;</p>

{% elseif entities is empty %}
    <p>Your search returned no results. Please try again.</p>
<p>&nbsp;</p>
<p>&nbsp;</p>

{% else %}
    <div class="projects-wrap">
        {% for entity in entities %}
            <div class="box">
                <div class="box-inner">
                    <div class="info-box">{{ entity.title }}</div>
                    <div class="info-box">{% if entity.startdate %}{{ entity.startdate|date('Y-m-d') }}{% endif %}</div>
                    <div class="info-box">
                        <a href="{{ path('project_proposal_new', { 'projectid': entity.id }) }}">Apply For This Project</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

{#         <ul>
        <li>
            <a href="{{ path('project_new') }}">
                Create a new entry
            </a>
        </li>
    </ul> #}
{% endif %}
{% endblock %}

